/**
 * Kafka服务器JAAS配置文件
 * 用于SCRAM-SHA-512认证机制
 * 注意：Kafka与Zookeeper之间不使用SASL认证
 */

// Kafka服务器配置 - 用于broker间通信和客户端认证
KafkaServer {
    org.apache.kafka.common.security.scram.ScramLoginModule required
    username="admin"
    password="Ye24_92xMG%iw@vT";
};

// 注意：不配置Client和zookeeper段，因为Kafka与Zookeeper之间不使用SASL认证